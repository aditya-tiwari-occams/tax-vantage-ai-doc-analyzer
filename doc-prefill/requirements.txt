# Stage-9 document prefill — dependencies
pymupdf>=1.24          # PDF text extraction (reading order)
pdfplumber>=0.11       # PDF table recovery
python-docx>=1.1       # DOCX (order-preserving walk)
mammoth>=1.7           # DOCX -> HTML fallback (inline formatting)
openai>=1.40           # AsyncOpenAI + Structured Outputs
python-dotenv>=1.0     # load OPENAI_API_KEY from .env
fastapi>=0.110         # web API + static serving
uvicorn[standard]>=0.27  # ASGI server
python-multipart>=0.0.9  # file uploads for FastAPI
reportlab>=4.0         # only used by the sample/test generators
